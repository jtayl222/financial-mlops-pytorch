resources:
  - ../../base
  - mlserver-hpa.yaml

patches:
  - path: increase-capacity-patch.yaml
    target:
      group: mlops.seldon.io
      version: v1alpha1
      kind: SeldonRuntime
      name: financial-mlops-runtime
      namespace: financial-mlops-pytorch